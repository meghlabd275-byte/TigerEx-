# TigerEx Complete Backend Requirements v11.0.0
# Comprehensive dependencies for all backend services

# ============= CORE FRAMEWORKS =============
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# ============= BUILD TOOLS (required for compiling some packages) =============
cython==3.0.10

# ============= DATABASES & ORM =============
sqlalchemy==2.0.23
alembic==1.13.1
psycopg2-binary==2.9.11
pymongo==4.10.1
# redis==5.0.1  # Let pip resolve
cassandra-driver==3.30.0

# ============= AUTHENTICATION & SECURITY =============
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
cryptography==44.0.0
oauthlib==3.2.2
authlib==1.3.0

# ============= HTTP CLIENTS & API =============
httpx==0.25.2
aiohttp==3.9.1
requests>=2.31.0
# urllib3 - Let pip resolve conflicts automatically
websockets>=12.0

# ============= ASYNC & CONCURRENCY =============
asyncio==3.4.3
celery==5.3.4
kombu==5.3.4
confluent-kafka>=2.3.0
pika==1.3.2

# ============= DATA PROCESSING & ANALYTICS =============
pandas>=2.2.3
# numpy==2.2.6  # Let pip resolve version based on dependencies
scipy>=1.17.1
scikit-learn>=1.6.1
ta>=0.10.2

# ============= CRYPTOCURRENCY & TRADING =============
ccxt>=4.1.90
python-binance==1.0.19
krakenex==2.1.0
pybit==5.7.0
# okx - unavailable for Python 3.13
# kucoin-python, gemini, coinbaseadvancedclient removed - no Python 3.13 wheels

# ============= FINANCIAL & MARKET DATA =============
yfinance>=0.2.28
pandas-market-calendars>=4.4.1
pyportfolioopt>=1.5.5

# ============= MONITORING & LOGGING =============
prometheus-client==0.19.0
statsd==4.0.1
structlog==23.2.0
loguru==0.7.2
sentry-sdk>=1.38.0

# ============= TASK QUEUE & JOB SCHEDULING =============
apscheduler==3.10.4
rq==1.15.1
# celery-beat, dramatiq removed - no Python 3.13 wheels

# ============= VALIDATION & SERIALIZATION =============
marshmallow==3.20.1
cerberus==1.3.5
jsonschema==4.20.0

# ============= EMAIL & NOTIFICATIONS =============
aiosmtplib==3.0.1
sendgrid==6.10.0
twilio==8.10.0
fcm-django==1.0.9
pusher>=3.3.2

# ============= FILE PROCESSING & STORAGE =============
pillow==11.3.0
python-magic==0.4.27
boto3==1.34.0
minio>=7.2.0
aiofiles==23.2.1

# ============= RATE LIMITING & CACHING =============
slowapi==0.1.9
redis-lru==0.1.1
hiredis==2.2.3
diskcache==5.6.3

# ============= TESTING & QUALITY =============
pytest>=7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
black>=23.11.0
isort>=5.12.0
flake8>=6.1.0
mypy>=1.7.1

# ============= DEVELOPMENT TOOLS =============
jupyter==1.0.0
ipython==8.17.2
watchdog==3.0.0
python-dotenv==1.0.0

# ============= API DOCUMENTATION =============
fastapi-users==12.1.2
fastapi-pagination==0.12.13
fastapi-limiter==0.1.5
fastapi-cache==0.1.0
# grpcio removed - compilation issues

# ============= MESSAGING & COMMUNICATION =============
# grpcio removed - compilation issues
protobuf==4.25.1
thrift==0.20.0

# ============= TIME & DATE HANDLING =============
pytz==2023.3
# dateutil removed - no longer available

# ============= CONFIGURATION MANAGEMENT =============
dynaconf==3.2.4
hydra-core>=1.3.2
configparser==6.0.0
pyyaml==6.0.1

# ============= GEOGRAPHIC & LOCATION =============
geopy==2.4.1
geoip2>=4.8.0
country-converter==1.2.0

# ============= BLOCKCHAIN & WEB3 =============
web3==6.11.3
py-solc-x==1.1.1
slither-analyzer>=0.9.6
eth-account>=0.9.0

# ============= SECURITY & ENCRYPTION =============
cryptography==44.0.0
pycryptodome==3.19.0
bcrypt==4.1.2
argon2-cffi==23.1.0

# ============= PERFORMANCE OPTIMIZATION =============
orjson==3.9.10
msgpack>=1.0.7
# pickle5 removed as part of Python 3.13 workarounds

# ============= DATABASE MIGRATIONS & SEEDING =============
alembic==1.13.1
# seed-faker removed - unavailable for Python 3.13
factory-boy==3.3.0

# ============= WEBSOCKET & REAL-TIME =============
# socketio, eventlet, gevent removed - unavailable for Python 3.13

# ============= IMAGE PROCESSING =============
opencv-python-headless>=4.8.1.78
imageio>=2.33.1
# skimage removed - unavailable for Python 3.13

# ============= NATURAL LANGUAGE PROCESSING =============
nltk==3.8.1
# (spacy, transformers removed - Python 3.13 compatibility)
openai==1.3.8

# ============= SCRAPING & AUTOMATION =============
beautifulsoup4==4.12.2
selenium>=4.16.0
scrapy>=2.11.0
playwright>=1.50.0

# ============= HEALTH CHECKS & MONITORING =============
# health-check removed - unavailable for Python 3.13
psutil==5.9.6
GPUtil==1.4.0
py-cpuinfo==9.0.0

# ============= ENVIRONMENT VARIABLES & SECRETS =============
python-decouple==3.8
keyring==24.3.0
# vault - conflicts with Babel

# ============= BACKUP & RECOVERY =============
# pgbackup, mongo-backup, s3-backup removed - unavailable for Python 3.13

# ============= API TESTING =============
httpx==0.25.2
requests-mock==1.11.0
responses>=0.24.1
vcrpy==6.0.1

# ============= DATA MIGRATION =============
pandas-gbq>=0.19.2
sqlalchemy2-stubs==0.0.2a38
pymysql==1.1.0

# ============= MICROSERVICES =============
# nameko removed - unavailable for Python 3.13
flask==3.0.0
django==4.2.7

# ============= MACHINE LEARNING DEPLOYMENT =============
# (mlflow, bentoML, kserve removed - Python 3.13 compatibility)

# ============= CONTAINER ORCHESTRATION =============
docker>=6.1.3
kubernetes>=28.1.0
openshift==0.13.1

# ============= CLI TOOLS =============
click==8.1.7
typer==0.9.0
rich==13.7.0

# ============= ASYNC DATABASE DRIVERS =============
asyncpg>=0.29.0
aiomysql==0.2.0
motor==3.3.2
# aioredis==2.0.1  # Let pip resolve, also needed by fastapi-cache

# ============= GRAPH DATABASES =============
neo4j==5.15.0
py2neo>=2021.2.4
gremlinpython==3.7.1

# ============= SEARCH ENGINES =============
elasticsearch==8.11.0
opensearch-py>=2.4.0
whoosh==2.7.4

# ============= TIME SERIES DATABASES =============
influxdb-client>=1.39.0
# timescaledb-psycopg2 removed - unavailable for Python 3.13
clickhouse-driver>=0.2.6

# ============= EVENT STREAMING =============
faust==1.10.4; python_version < "3.11"
# pykka removed - unavailable for Python 3.13
ray>=2.45.0

# ============= DISTRIBUTED COMPUTING =============
dask>=2023.11.0
joblib==1.3.2
distributed>=2023.11.0

# ============= MESSAGING PROTOCOLS =============
mqtt==0.0.1
amqp==5.2.0
stomp.py==8.1.0

# ============= DOCUMENTATION =============
mkdocs>=1.5.3
mkdocs-material==9.4.8
sphinx>=7.2.6

# ============= API GATEWAY =============
# kong - using latest available version (API gateway, not Python package)
# kong==0.14.0  # No longer available, using kongpy or direct API
# tyk==2.2.0  # Tyk API Gateway - separate product, not a Python package
# ambassador==0.1.0  # Ambassador API Gateway - separate product

# ============= SERVICE MESH =============
# istio==1.19.0  # Istio service mesh - not a Python package
# linkerd==2.10.0  # Linkerd service mesh - not a Python package
# consul==1.1.10  # Consul by HashiCorp - not a Python package

# ============= OBSERVABILITY =============
jaeger-client==4.8.0
zipkin==0.1.0  # Available version
opencensus==0.11.3

# ============= LOAD BALANCING =============
# nginx==1.25.3  # NGINX web server - not a Python package
# haproxy==2.8.0  # HAProxy - not a Python package
# traefik==2.10.0  # Traefik - not a Python package

# ============= CERTIFICATE MANAGEMENT =============
certbot>=2.7.4
acme>=2.7.4
cryptography==44.0.0

# ============= COMPLIANCE & REGULATION =============
pycryptodome==3.19.0
shamir-mnemonic==0.3.0
mnemonic==0.20

# ============= FINANCIAL COMPLIANCE =============
plaid-python==12.0.0
stripe==7.6.0
adyen==15.0.1

# ============= TRADING ALGORITHMS =============
# (bt, zipline, backtrader removed - Python 3.13 compatibility issues)

# ============= RISK MANAGEMENT =============
# (pyfolio, empyrical, ffn removed - Python 3.13 compatibility issues)

# ============= PORTFOLIO OPTIMIZATION =============
pyportfolioopt>=1.5.5
cvxpy>=1.3.2
# riskparity==1.0.0  # Not available on PyPI

# ============= TECHNICAL ANALYSIS =============
TA-Lib>=0.4.28
ta>=0.10.2
pandas-ta>=0.4.71b0

# ============= MARKET DATA FEEDS =============
yfinance>=0.2.28
alpha_vantage==2.3.1
iexfinance==0.4.3
# polygon-api-client - conflicts with websockets

# ============= SOCIAL TRADING =============
# twitter-api-v2==1.1.0  # Not available, use python-twitter
telegram==0.0.1
discord-webhook==1.3.0

# ============= BACKUP & DISASTER RECOVERY =============
# pg_dump - PostgreSQL dump tool, not a Python package
# mongodump - MongoDB dump tool, not a Python package
# rclone==1.64.2  # Cloud storage sync tool, not a Python package

# ============= DEVELOPMENT ENVIRONMENTS =============
jupyterlab==4.0.9
# vscode-python==2023.22.1  # VS Code extension, not a Python package
# docker-compose==1.29.2  # Docker tool, not a Python package

# ============= PRODUCTION DEPLOYMENT =============
gunicorn==19.3.0
supervisor>=4.2.5
# systemd-python==235  # Requires systemd development libraries

# ============= MONITORING DASHBOARDS =============
grafana-api==1.0.3
# kibana-api==1.0.0  # Use elasticsearch-py or opensearch-py instead
prometheus-api==0.0.1

# ============= LOGGING & ANALYTICS =============
# splunk-sdk==1.7.4  # Fails to build on Python 3.13
elastic-apm==6.16.1
datadog==0.47.0

# ============= SECURITY SCANNING =============
bandit==1.7.5
safety>=2.3.5
pip-audit==2.6.1

# ============= DEPENDENCY MANAGEMENT =============
pip-tools>=7.3.0
pipdeptree==2.13.0
pip-check-updates==0.28.0

# ============= CODE QUALITY =============
pre-commit==3.6.0
pylint==3.0.3
autopep8==2.0.4

# ============= DOCUMENTATION GENERATION =============
pdoc==14.3.0
autodocsumm==0.2.11
# sphinx-rtd-theme - conflicts with sphinx docutils

# ============= API DOCUMENTATION =============
redoc==0.2.3
swagger-ui-bundle==0.0.9
prance==23.6.21.0

# ============= CONTAINER SECURITY =============
# trivy - Container security scanner, not a Python package
# clair==4.6.0  # Container vulnerability scanner, not a Python package
# anchore==0.9.2  # Container analysis platform, not a Python package

# ============= INFRASTRUCTURE AS CODE =============
# terraform - Infrastructure as Code tool, not a Python package
# pulumi==3.108.0  # Pulumi SDK, not available on PyPI
# ansible==8.7.0  # Ansible automation platform, not a Python package

# ============= CI/CD PIPELINES =============
jenkinsapi==0.3.22  # Latest available version
python-gitlab==3.14.0
github3.py==4.0.1

# ============= VERSION CONTROL =============
gitpython==3.1.40
semantic-version==2.10.0
bump2version==1.0.1

# ============= PACKAGE MANAGEMENT =============
poetry==1.7.1
pipenv>=2023.11.15
# conda==23.11.0  # Conda package manager, not a Python package

# ============= VIRTUALIZATION =============
virtualenv==20.24.7
# pyenv==2.3.30  # Python version manager, not a Python package
tox==4.11.4

# ============= CONTAINER TOOLS =============
docker>=6.1.3
podman==5.8.0
# buildah==1.32.0  # Container build tool, not a Python package

# ============= ORCHESTRATION =============
kubernetes>=28.1.0
# helm==3.13.0  # Kubernetes Helm package manager, different from PyPI helm
# kubectl==1.29.0  # Kubernetes CLI tool, not a Python package

# ============= MONITORING =============
prometheus-client==0.19.0
statsd==4.0.1
graphyte==1.7.1

# ============= TRACING =============
opentelemetry-api==1.21.0
jaeger-client==4.8.0
zipkin==0.1.0  # Available version

# ============= LOGGING AGENTS =============
# fluentd-logger - conflicts with setuptools
# logstash-async==2.8.0  # Logstash async logging, not a Python package
# vector-client==0.1.0  # Vector observability pipeline, not available

# ============= METRICS ===============
datadog==0.47.0
newrelic==12.1.0
appdynamics==26.4.0.8689

# ============= PROFILING ==============
pyinstrument==4.6.2
memory-profiler==0.61.0
# line-profiler==4.1.1  # Requires unavailable Cython version

# ============= DEBUGGING =============
pdbpp==0.10.3
ipdb==0.13.13
pudb==2023.1

# ============= PERFORMANCE ==============
pyinstrument==4.6.2
memory-profiler==0.61.0
# numba==0.58.1  # Does not support Python 3.13
# pythran==0.14.0  # Requires numba

# ============= PARALLEL COMPUTING =============
multiprocessing-logging==0.3.4
joblib==1.3.2
# concurrent-futures==3.1.1  # Part of Python stdlib since 3.2

# ============= DISTRIBUTED SYSTEMS =============
# pykka removed - unavailable for Python 3.13
ray>=2.45.0
dask>=2023.11.0

# ============= CLOUD PROVIDERS =============
boto3==1.34.0
google-cloud==0.34.0
# azure-sdk==1.0.0  # Azure SDK, use individual packages instead