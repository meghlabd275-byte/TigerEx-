{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/out",
  "installCommand": "npm install",
  "framework": "nextjs",
  "routes": {
    "/api/**": {
      "origin": "https://tigerex-api.onrender.com"
    }
  }
}
